export ADHOC_ODB_PASSWORD=z6X9HHBzfPJCMHYR

if [ x"$CHALMERS_DEPLOY_LEVEL" == x"devel" ]; then
    export ADHOC_DB_PASSWORD=WWW
fi

if [ x"$CHALMERS_DEPLOY_LEVEL" == x"uat" ]; then
    export ADHOC_DB_PASSWORD=XXX
fi

if [ x"$CHALMERS_DEPLOY_LEVEL" == x"prod" ]; then
    export ADHOC_DB_PASSWORD=YYY
fi

if [ x"$CHALMERS_DEPLOY_LEVEL" == x"install" ]; then
    export ADHOC_DB_PASSWORD=ZZZ
fi